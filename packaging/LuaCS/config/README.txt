LuaCS configuration files belong in this directory.

compile.exe also stores the installation key here as luacs.key. Keep that key with the .smg packages created by the same installation. Replacing or deleting it can make existing plugin packages unreadable.

Individual Lua plugins may place their own configuration files here if they choose.
