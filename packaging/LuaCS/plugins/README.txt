compile.exe writes authenticated .smg plugin packages to this directory.

Keep config\luacs.key with packages compiled by this installation. The runtime needs the same key to authenticate and decrypt them.

example_welcome.lua is installed under scripting as source documentation. It is not compiled or auto-loaded during deployment.
